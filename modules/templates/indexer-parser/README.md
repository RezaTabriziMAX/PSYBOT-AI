
# indexer-parser (Template)

A template for building parsers that:
- consume Solana log messages / tx metadata
- normalize events into a consistent JSON shape
- validate and version the output schema

This is intended to be used by an indexer worker.
